{
  "compilerOptions": {
    "baseUrl": ".", // This line is important to resolve modules based on the root directory
    "paths": {
      "@/pages/*": ["src/pages/*"],
      "@/components/*": ["src/components/*"],
      "@/utils/*": ["src/utils/*"],
      // Add more as needed for other directories within `src`
    }
  }
}
